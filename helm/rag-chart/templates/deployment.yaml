apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rag-chart.fullname" . }}-{{ .Chart.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "rag-chart.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "rag-chart.fullname" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
